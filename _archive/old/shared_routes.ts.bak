
import { type } from 'arktype';
import { insertUserSchema, users } from './schema';

export const api = {
  health: {
    check: {
      method: 'GET' as const,
      path: '/api/health',
      responses: {
        200: type({ status: "string" }),
      },
    },
  },
};
