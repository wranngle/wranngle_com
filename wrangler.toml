name = "wranngle_com"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Environment variables are configured in the Cloudflare Pages
# Dashboard (Settings → Environment variables). Required: N8N_WEBHOOK_URL.
